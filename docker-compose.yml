version: "3.9"

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: screenflow
      POSTGRES_PASSWORD: screenflow
      POSTGRES_DB: screenflow
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    environment:
      POSTGRES_USER: screenflow
      POSTGRES_PASSWORD: screenflow
      POSTGRES_DB: screenflow
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
    env_file: .env
    depends_on:
      - db
    volumes:
      - ./media:/app/media
      - ./backend/app:/app/app
      - ./license.json:/app/license.json
      - ./license_public.pem:/app/license_public.pem
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    ports:
      - "8000:8000"

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    environment:
      VITE_API_URL: http://backend:8000
    depends_on:
      - backend
    ports:
      - "3000:3000"

volumes:
  db_data:

